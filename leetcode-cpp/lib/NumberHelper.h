#include <vector>
#include <iostream>
#include <climits>
#include <algorithm>
#include <queue>
#include <stack>
#include <map>

using namespace std;

// use Array to Simulate Queue
class NumberHelper {
public:
    string NumberToBinary(int N);
    string thousandSeparator(int n);
    int MySqrt(int x);
    int MainEntry();
};