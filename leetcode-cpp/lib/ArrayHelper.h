#include<vector>
#include<iostream>
#include <algorithm>
using namespace std;

class ArrayHelper {
public:
    vector<int> revert(vector<int> c);
    int maxSubArray(vector<int> c);
    void MainEntry();
};